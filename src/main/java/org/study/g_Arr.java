package org.study;
/*
    final:
        1. final int MAX_SPEED=120   // 修饰变量，使变量成为常量
        2. final void study()P{}     // 修饰方法，不能被子类重写，但可以重载
        3. final class A{}           // 修饰类，类不能被继承

    数组：也是对象，是相同数据的有序集合
        1. 一旦创建，长度确定
            int[] arr = new int[10];
            String[] arr = new String[5];
            类[] arr = new 类[3];  // 这里数组放的是地址，并未创建对象。
        2. 数组初始化
            静态初始化： int[] a = {2,4,6,5}
            默认初始化：
            动态初始化： 挨个赋值
        3. 数组遍历
            for each 专门用来读取数组中的值，不能修改


 */
public class g_Arr {
}
